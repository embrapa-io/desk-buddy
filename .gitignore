# PlatformIO
.pio/
.pioenvs/
.piolibdeps/

# IDE
.vscode/
.idea/
*.code-workspace

# Build artifacts
*.bin
*.elf
*.map
# binário público do gravador web (distribuição, não é artefato de build)
!docs/firmware/*.bin

# OS
.DS_Store
Thumbs.db

# Instruções locais do harness (context-mode) — não fazem parte do produto
CLAUDE.md

# Credenciais (WiFi etc.) — nunca versionar
include/secrets.h

# Node (gerador de assets de logo) e PNGs intermediários
node_modules/
package.json
package-lock.json
tools/*.png
